name: release

on:
  push:
    branches:
      - main
    tags:
      - '*'
jobs:
  build:
    runs-on: ubuntu-24.04
      - name: release
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          echo "Tag name: ${{ github.event.release.tag_name }}"

