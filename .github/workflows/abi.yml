name: ABI

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  call-workflow-abi_c:
    uses: ./.github/workflows/abi_c.yml
    with:
      old: '14.3'
      new: '14.4'
      dev: '15.0'

