name: conda

on:
  push:
    branches:
      - main

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: miniconda
        uses: conda-incubator/setup-miniconda@v3
      - name: build-conda
        run: |
          conda install -y conda-build
          conda-build .

