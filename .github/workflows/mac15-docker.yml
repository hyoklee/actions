name: mac-15 docker

on:
  push:
    branches: main

jobs:
  build:
    name: docker
    runs-on: macos-15
    steps:
      - name: install
        run: |
          brew install docker
          brew install colima
          brew services start colima
          docker version
          docker model status
