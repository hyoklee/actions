name: sol gcc

on:
  push:
    branches: main

defaults:
  run:
    shell: bash
jobs:
  build:
    name: solaris-gcc
    runs-on: ubuntu-latest
    steps:
      - name: test
        uses: vmactions/solaris-vm@v1
        with:
          envs: 'MYTOKEN MYTOKEN2'
          release: "11.4-gcc"
          usesh: true
          prepare: |
            pkg install gnu-tar
          run:
            env
            ls
            echo "OK"
#            curl -O -L https://github.com/HDFGroup/hdf5/archive/refs/tags/snapshot-1.14.tar.gz
#            gtar zxvf snapshot-1.14.tar.gz
#            cd hdf5-snapshot-1.14
#            mkdir build
#            cd build
#            cmake -DSITE:STRING="sol-11.4" -DBUILDNAME:STRING="4/r" -DCTEST_DROP_SITE_INIT:STRING="my.cdash.org" -DCTEST_DROP_LOCATION_INIT:STRING="/submit.php?project=HDF5" ..
#            ctest -D Experimental -C Release
