name: mac-13 snapshot devel

on:
  push:
    branches: main

jobs:
  build:
    name: Test snapshot-devel
    runs-on: macos-13
    steps:
      - name: test
        run: |
          curl -O -L https://github.com/HDFGroup/hdf5/archive/refs/tags/snapshot.tar.gz
          tar zxf snapshot.tar.gz
          cd hdf5-snapshot
          cmake --workflow --preset=ci-StdShar-OSX-Clang --fresh	  
          # mkdir build
          # cd build
          # cmake -DSITE:STRING="mac-13" -DBUILDNAME:STRING="4/r" -DCTEST_DROP_SITE_INIT:STRING="my.cdash.org" -DCTEST_DROP_LOCATION_INIT:STRING="/submit.php?project=HDF5" ..
          # ctest -D Experimental -C Release

