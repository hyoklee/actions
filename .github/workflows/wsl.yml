name: wsl

on:
  push:
    branches:
      - main
jobs:
  build:
    strategy:
      matrix:
        os: [kali-linux, openSUSE-Leap-15.5]
    runs-on: windows-latest
    steps:
      - name: Run the script
        run: |
          wsl --install --online -d ${{ matrix.os }}
