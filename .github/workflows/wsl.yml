name: wsl

on:
  push:
    branches:
      - main
jobs:
  build:
    strategy:
      matrix:
        os: [kali-linux, openSUSE]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Run the script
        run: |
          wsl --install ${{ matrix.os }}
