name: wsl

on:
  push:
    branches:
      - main
jobs:
  build:
    strategy:
      matrix:
        os: [kali-linux, openSUSE]
    runs-on: windows-latest
    steps:
      - name: Run the script
        run: |
          wsl --install -d ${{ matrix.os }}
