name: pip

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: test
        run: |
          pip install iowarp
          chimaera_start_runtime
#          sudo apt install libyaml-cpp-dev
#          pip install iowarp-core
#          wrp_cae_omni
