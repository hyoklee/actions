name: win awvwgk/setup-fortran

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Install Intel OneAPI
        uses: awvwgk/setup-fortran@v1
      - name: Install MPI
        uses: mpi4py/setup-mpi@v1
        with:
          mpi: intelmpi
