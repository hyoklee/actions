name: lin lmod

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          sudo apt -y install environment-modules
          ls /usr/share/modules/init/
          eval "$(SHELL=/bin/bash module shinit)"
          which module
          module list
