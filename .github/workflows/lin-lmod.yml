name: lin lmod

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          sudo apt -y install lmod
          eval "$(SHELL=/bin/bash module shinit bash)"
          which module
          module list
