name: lin lmod

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: test
        shell: bash
        run: |
          sudo apt -y install lmod
          which module
          module list
