name: lin lmod

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          sudo apt -y install environment-modules
          source /etc/profile.d/modules.sh
          which module
          module list
