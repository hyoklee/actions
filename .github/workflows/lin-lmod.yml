name: lin lmod

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: test
      run: |
        sudo apt -y install lmod coreutils
        which which
        tree /etc/profile.d
