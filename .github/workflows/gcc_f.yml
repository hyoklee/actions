name: gcc f

on:
  push:
    branches: main

jobs:
  test: # make sure the action works on a clean machine without building
    name: ${{ matrix.os.name }} ${{ matrix.os.architecture }} ${{ matrix.os.version }} on ${{ matrix.os.host }}
    runs-on: ${{ matrix.os.host }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - name: openbsd
            architecture: x86-64
            version: '7.8'
            host: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: brtnfld/hdf5
          ref: H5R

      - name: ${{ matrix.os.name }}
        uses: cross-platform-actions/action@master
        env:
          FOO: A
          BAR: B
        with:
          environment_variables: FOO BAR
          operating_system: ${{ matrix.os.name }}
          architecture: ${{ matrix.os.architecture }}
          version: '${{ matrix.os.version }}'
          shell: bash
          run: |
            sudo pkg_add git
            sudo pkg_add cmake
            sudo pkg_add g95-11.2.0p19
            export FC=/usr/local/bin/egfortran
            export CC=/usr/local/bin/egcc
            mkdir build
            cd build
            cmake \
            -D BUILDNAME="brtnfld/gcc/f" \
            -D CTEST_DROP_SITE_INIT="my.cdash.org" \
            -D HDF5_BUILD_FORTRAN=ON \
            -D SITE=obsd-7.8 \
            ..
            ctest -T Build
            ctest -VVV --output-on-failure -T Test -R FORTRAN_testhdf5_fortran
            

